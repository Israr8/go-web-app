apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: go-lang.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: golang-service
                port:
                  number: 80
